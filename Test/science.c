#include <stdio.h>

/* I am writing a program that converts a number to its scientific notation */

double main ()
{
    double dbl_num = 123.456;

    printf("The floating point value is: %f\n", dbl_num);
    printf("The science notation value is: %e\n", dbl_num);

    return 0;
}