#include <stdio.h>
/**
* main - this function converts decimals 
to hexadecimals.*/

int main()
{
    int num = 16;

    for (num == 16; num < 21; num ++)
        printf("The decimal of num is %d, and its hex is: %x\n", num, num);
    return 0;
}